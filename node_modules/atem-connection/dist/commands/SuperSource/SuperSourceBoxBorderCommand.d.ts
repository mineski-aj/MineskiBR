/// <reference types="node" />
import { DeserializedCommand, WritableCommand } from '../CommandBase';
import { AtemState } from '../../state';
import { SuperSourceBoxBorder } from '../../state/video/superSource';
import { ProtocolVersion } from '../../enums';
/**
 * Reports the per-box SuperSource border on the Constellation HD range and
 * newer (firmware 9.6.0+). See {@link SuperSourceBoxBorderCommand} for the
 * write side.
 */
export declare class SuperSourceBoxBorderUpdateCommand extends DeserializedCommand<{
    ssrcId: number;
    boxId: number;
    border: SuperSourceBoxBorder;
}> {
    static readonly rawName = "SSSB";
    static readonly minimumVersion = ProtocolVersion.V9_6;
    static deserialize(rawCommand: Buffer): SuperSourceBoxBorderUpdateCommand;
    applyToState(state: AtemState): string;
}
/**
 * Sets the per-box SuperSource border on the ATEM Constellation HD range and
 * newer (firmware 9.6.0+); {@link SuperSourceBoxBorderUpdateCommand} reports
 * it back. Older models use {@link SuperSourceBorderCommand} instead - the two
 * ranges each ignore the other's command.
 */
export declare class SuperSourceBoxBorderCommand extends WritableCommand<SuperSourceBoxBorder> {
    static MaskFlags: {
        borderEnabled: number;
        borderWidthOutVertical: number;
        borderWidthOutHorizontal: number;
        borderWidthInLeft: number;
        borderWidthInRight: number;
        borderWidthInTop: number;
        borderWidthInBottom: number;
        borderHue: number;
        borderSaturation: number;
        borderLuma: number;
    };
    static readonly rawName = "CSSB";
    static readonly minimumVersion = ProtocolVersion.V9_6;
    readonly ssrcId: number;
    readonly boxId: number;
    constructor(ssrcId: number, boxId: number);
    serialize(): Buffer;
}
//# sourceMappingURL=SuperSourceBoxBorderCommand.d.ts.map