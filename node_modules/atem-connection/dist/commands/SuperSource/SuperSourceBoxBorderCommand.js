"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SuperSourceBoxBorderCommand = exports.SuperSourceBoxBorderUpdateCommand = void 0;
const CommandBase_1 = require("../CommandBase");
const state_1 = require("../../state");
const enums_1 = require("../../enums");
// SSSB/CSSB transmit border widths across the full u16 range (0xffff = 16.00),
// whereas this library expresses widths in hundredths (0-1600), matching the
// documented SuperSourceBorder/CSBd convention. Convert at the protocol
// boundary so both border types use the same scale in state.
const WIDTH_API_MAX = 1600;
const WIDTH_WIRE_MAX = 0xffff;
const widthFromWire = (raw) => Math.round((raw * WIDTH_API_MAX) / WIDTH_WIRE_MAX);
const widthToWire = (val) => Math.round((val * WIDTH_WIRE_MAX) / WIDTH_API_MAX);
/**
 * Reports the per-box SuperSource border on the Constellation HD range and
 * newer (firmware 9.6.0+). See {@link SuperSourceBoxBorderCommand} for the
 * write side.
 */
class SuperSourceBoxBorderUpdateCommand extends CommandBase_1.DeserializedCommand {
    static deserialize(rawCommand) {
        return new SuperSourceBoxBorderUpdateCommand({
            ssrcId: rawCommand.readUInt8(0),
            boxId: rawCommand.readUInt8(1),
            border: {
                borderEnabled: rawCommand.readUInt8(2) === 1,
                borderWidthOutVertical: widthFromWire(rawCommand.readUInt16BE(4)),
                borderWidthOutHorizontal: widthFromWire(rawCommand.readUInt16BE(6)),
                borderWidthInLeft: widthFromWire(rawCommand.readUInt16BE(8)),
                borderWidthInRight: widthFromWire(rawCommand.readUInt16BE(10)),
                borderWidthInTop: widthFromWire(rawCommand.readUInt16BE(12)),
                borderWidthInBottom: widthFromWire(rawCommand.readUInt16BE(14)),
                borderHue: rawCommand.readUInt16BE(16),
                borderSaturation: rawCommand.readUInt16BE(18),
                borderLuma: rawCommand.readUInt16BE(20),
            },
        });
    }
    applyToState(state) {
        if (!state.info.capabilities || this.properties.ssrcId >= state.info.capabilities.superSources) {
            throw new state_1.InvalidIdError('SuperSource', this.properties.ssrcId);
        }
        const supersource = state_1.AtemStateUtil.getSuperSource(state, this.properties.ssrcId);
        const box = state_1.AtemStateUtil.getSuperSourceBox(supersource, this.properties.boxId);
        box.border = this.properties.border;
        return `video.superSources.${this.properties.ssrcId}.boxes.${this.properties.boxId}.border`;
    }
}
exports.SuperSourceBoxBorderUpdateCommand = SuperSourceBoxBorderUpdateCommand;
SuperSourceBoxBorderUpdateCommand.rawName = 'SSSB';
SuperSourceBoxBorderUpdateCommand.minimumVersion = enums_1.ProtocolVersion.V9_6;
/**
 * Sets the per-box SuperSource border on the ATEM Constellation HD range and
 * newer (firmware 9.6.0+); {@link SuperSourceBoxBorderUpdateCommand} reports
 * it back. Older models use {@link SuperSourceBorderCommand} instead - the two
 * ranges each ignore the other's command.
 */
class SuperSourceBoxBorderCommand extends CommandBase_1.WritableCommand {
    constructor(ssrcId, boxId) {
        super();
        this.ssrcId = ssrcId;
        this.boxId = boxId;
    }
    serialize() {
        const buffer = Buffer.alloc(24);
        buffer.writeUInt16BE(this.flag, 0);
        buffer.writeUInt8(this.ssrcId, 2);
        buffer.writeUInt8(this.boxId, 3);
        buffer.writeUInt8(this.properties.borderEnabled ? 1 : 0, 4);
        // byte 5: padding for 2-byte alignment
        buffer.writeUInt16BE(widthToWire(this.properties.borderWidthOutVertical || 0), 6);
        buffer.writeUInt16BE(widthToWire(this.properties.borderWidthOutHorizontal || 0), 8);
        buffer.writeUInt16BE(widthToWire(this.properties.borderWidthInLeft || 0), 10);
        buffer.writeUInt16BE(widthToWire(this.properties.borderWidthInRight || 0), 12);
        buffer.writeUInt16BE(widthToWire(this.properties.borderWidthInTop || 0), 14);
        buffer.writeUInt16BE(widthToWire(this.properties.borderWidthInBottom || 0), 16);
        buffer.writeUInt16BE(this.properties.borderHue || 0, 18);
        buffer.writeUInt16BE(this.properties.borderSaturation || 0, 20);
        buffer.writeUInt16BE(this.properties.borderLuma || 0, 22);
        return buffer;
    }
}
exports.SuperSourceBoxBorderCommand = SuperSourceBoxBorderCommand;
SuperSourceBoxBorderCommand.MaskFlags = {
    borderEnabled: 1 << 0,
    borderWidthOutVertical: 1 << 1,
    borderWidthOutHorizontal: 1 << 2,
    borderWidthInLeft: 1 << 3,
    borderWidthInRight: 1 << 4,
    borderWidthInTop: 1 << 5,
    borderWidthInBottom: 1 << 6,
    borderHue: 1 << 7,
    borderSaturation: 1 << 8,
    borderLuma: 1 << 9,
};
SuperSourceBoxBorderCommand.rawName = 'CSSB';
SuperSourceBoxBorderCommand.minimumVersion = enums_1.ProtocolVersion.V9_6;
//# sourceMappingURL=SuperSourceBoxBorderCommand.js.map