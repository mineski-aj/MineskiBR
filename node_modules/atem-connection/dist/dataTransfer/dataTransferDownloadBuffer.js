"use strict";
var _DataTransferDownloadBuffer_data, _DataTransferDownloadBuffer_dataLength;
Object.defineProperty(exports, "__esModule", { value: true });
exports.DataTransferDownloadBuffer = void 0;
const tslib_1 = require("tslib");
const DataTransfer_1 = require("../commands/DataTransfer");
const dataTransfer_1 = require("./dataTransfer");
/**
 * Generic base for transfers which download a buffer from the ATEM.
 *
 * The data arrives as a sequence of `DataTransferDataCommand` packets, terminated by a
 * `DataTransferCompleteCommand`. A misbehaving or hostile device could stream data packets
 * indefinitely (or never send the completion), so each implementation must provide an upper
 * bound on the expected size. Once that bound is exceeded the transfer is aborted instead of
 * accumulating without limit until the process runs out of memory.
 */
class DataTransferDownloadBuffer extends dataTransfer_1.DataTransfer {
    /**
     * @param transferIndex Index reported back in the ack commands
     * @param maxBufferSize Hard cap on the total downloaded size, in bytes
     */
    constructor(transferIndex, maxBufferSize) {
        super();
        this.transferIndex = transferIndex;
        this.maxBufferSize = maxBufferSize;
        _DataTransferDownloadBuffer_data.set(this, []);
        _DataTransferDownloadBuffer_dataLength.set(this, 0
        /**
         * @param transferIndex Index reported back in the ack commands
         * @param maxBufferSize Hard cap on the total downloaded size, in bytes
         */
        );
    }
    async handleCommand(command, oldState) {
        if (command instanceof DataTransfer_1.DataTransferErrorCommand) {
            switch (command.properties.errorCode) {
                case DataTransfer_1.ErrorCode.Retry:
                    return this.restartTransfer(command.properties.transferId);
                case DataTransfer_1.ErrorCode.NotFound:
                    this.abort(new Error('Invalid download'));
                    return {
                        newState: dataTransfer_1.DataTransferState.Finished,
                        commands: [],
                    };
                default:
                    // Abort the transfer.
                    this.abort(new Error(`Unknown error ${command.properties.errorCode}`));
                    return {
                        newState: dataTransfer_1.DataTransferState.Finished,
                        commands: [],
                    };
            }
        }
        else if (command instanceof DataTransfer_1.DataTransferDataCommand) {
            tslib_1.__classPrivateFieldGet(this, _DataTransferDownloadBuffer_data, "f").push(command.properties.body);
            tslib_1.__classPrivateFieldSet(this, _DataTransferDownloadBuffer_dataLength, tslib_1.__classPrivateFieldGet(this, _DataTransferDownloadBuffer_dataLength, "f") + command.properties.body.length, "f");
            // Guard against a device streaming an unbounded amount of data (or never completing the
            // transfer), which would otherwise grow this buffer until the process runs out of memory.
            if (tslib_1.__classPrivateFieldGet(this, _DataTransferDownloadBuffer_dataLength, "f") > this.maxBufferSize) {
                this.abort(new Error('Download exceeded maximum expected size'));
                return {
                    newState: dataTransfer_1.DataTransferState.Finished,
                    commands: [],
                };
            }
            return {
                newState: oldState,
                commands: [
                    new DataTransfer_1.DataTransferAckCommand({
                        transferId: command.properties.transferId,
                        transferIndex: this.transferIndex,
                    }),
                ],
            };
        }
        else if (command instanceof DataTransfer_1.DataTransferCompleteCommand) {
            this.resolvePromise(Buffer.concat(tslib_1.__classPrivateFieldGet(this, _DataTransferDownloadBuffer_data, "f")));
            return {
                newState: dataTransfer_1.DataTransferState.Finished,
                commands: [],
            };
        }
        return { newState: oldState, commands: [] };
    }
}
exports.DataTransferDownloadBuffer = DataTransferDownloadBuffer;
_DataTransferDownloadBuffer_data = new WeakMap(), _DataTransferDownloadBuffer_dataLength = new WeakMap();
//# sourceMappingURL=dataTransferDownloadBuffer.js.map