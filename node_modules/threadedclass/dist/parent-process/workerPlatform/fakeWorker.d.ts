import { EncodingStrategy, Message } from '../../shared/sharedApi';
import { WorkerPlatformBase } from './_base';
export declare class FakeProcess extends WorkerPlatformBase {
    readonly encodingStrategy = EncodingStrategy.InProcess;
    private worker;
    constructor();
    kill(): void;
    send(m: Message.To.Any): void;
}
