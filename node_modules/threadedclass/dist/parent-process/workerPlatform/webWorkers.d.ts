import { EncodingStrategy, Message } from '../../shared/sharedApi';
import { WorkerPlatformBase } from './_base';
/** Functions for emulating child-process in web-workers */
export declare class WebWorkerProcess extends WorkerPlatformBase {
    readonly encodingStrategy = EncodingStrategy.StructuredClone;
    private worker;
    constructor(pathToWorker: string);
    kill(): void;
    send(message: Message.To.Any): void;
}
export declare function forkWebWorker(pathToWorker: string): WebWorkerProcess;
