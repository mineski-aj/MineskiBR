import { EncodingStrategy, Message } from '../../shared/sharedApi';
import { WorkerPlatformBase } from './_base';
/** Functions for spawning worker-threads in NodeJS */
export declare class WorkerThread extends WorkerPlatformBase {
    readonly encodingStrategy = EncodingStrategy.StructuredClone;
    private worker;
    constructor(pathToWorker: string);
    kill(): void;
    send(message: Message.To.Any): void;
}
export declare function forkWorkerThread(pathToWorker: string): WorkerThread;
